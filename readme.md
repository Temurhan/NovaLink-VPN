<div align="center">
  
  # NovaLink VPN Server v3.1 🚀

![Version](https://img.shields.io/badge/Version-3.1--beta-blueviolet?style=for-the-badge)
![C++](https://img.shields.io/badge/C++-20-blue?style=for-the-badge&logo=c%2B%2B)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)

Высокопроизводительный и защищенный VPN-сервер на C++, реализующий современный протокол **VLESS-Reality**. Проект ориентирован на скрытность трафика, автоматизацию и масштабируемость.
</div>


## ✨ Ключевые особенности
* **VLESS-Reality Implementation**: Маскировка VPN-трафика под стандартный TLS-поток (имитация посещения популярных сайтов через SNI).
* **High Performance Core**: Использование системного вызова `epoll` для асинхронной обработки тысяч параллельных соединений.
* **Manager-API**: Встроенный HTTP-сервер (порт 8080), отдающий актуальные настройки в формате JSON.
* **Auto-IP Discovery**: Сервер автоматически определяет свой публичный IP через внешние API при старте.
* **Automated Networking**: Программное создание TUN-интерфейса и настройка маршрутизации трафика (IP Forwarding).
* **Docker Ready**: Полная контейнеризация для быстрого деплоя на любой VPS за одну команду.

## 🛠 Технологический стек
* **Язык**: C++ 20 (используются современные стандарты ISO)
* **Сетевой стек**: `cpp-httplib`, `libcurl`, `OpenSSL`
* **База данных**: `SQLite3` (для хранения данных сессий)
* **CI/CD**: `GitHub Actions` для автоматической проверки сборки
* **Сборка**: `CMake` & `Make`



## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone [https://github.com/Temurhan/NovaLink-VPN.git](https://github.com/Temurhan/NovaLink-VPN.git)
cd NovaLink-VPN
```
2. Запуск через Docker

Убедитесь, что у вас установлены docker и docker-compose.
Bash

sudo docker-compose up --build -d

3. Получение конфигурации

Для подключения клиента запросите конфиг у Manager-API:
Bash

curl http://localhost:8080/api/v1/config

📂 Структура проекта

    /server: Ядро сервера, логика epoll и обработка TUN.

    /manager: API для управления и динамической выдачи конфигов.

    /client: Клиентские модули и реализация исходящих соединений (Outbound).

    /core: Общие библиотеки и заголовки.

🛡 Безопасность

Проект автоматически генерирует SSL-сертификаты при первом запуске (RSA 4096-bit). Все конфиденциальные данные (ключи, БД) добавлены в .gitignore и не попадают в публичный доступ.

Разработано с акцентом на приватность и производительность. 🛡️
